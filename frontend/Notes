Done:
Basic App page set up
Accepts coordinates
Returns real weather chart info
Added Map
Display peak values
Bolder text on chart for better readability
Docker improvements
change update button to save
improved peak values display
enabled user edits for weather data
peak values and route map updates when table is updated
added route name and vessel name user inputs
Added estimated travel time and travel distance

Added Regenerate Weather Situation feature
Added download feature
Added an upload JSON feature
Made the map reorient if the way points are moved
Improved Waypoint error msg handling
Improved comments
Changed Site page name and icon
Updated all mentions of "Ship" to "Vehicle"
Added some automated front end tests

Improved Wind Dir display with textbox-driven arrow and compass label
Added map wind direction arrows for each waypoint color based on wind speed
Moved waypoint chart to the bottom of the page (Might be better layout for large routes)
real generated Forecast Discussion text added
Added some general front end tests
Pre determined weather validation test
Improve wind direction on map display
Accounted for missing wind data causing issues with map
Flipped wind arrows to point to wind direction instead of wind source
Added more special cases to account for missing/0 Wind MPH and Wind Dir
-
Removed 180° rotation for wind so arrows point in standard direction.
Implemented Wind barbs inplace of arrows
Joined the way point and Wind barb to make it easer to read
removed save button as this implementation is not needed
added secondary regenerate and download buttons to the bottom of the waypoint chart
Made the lower regenerate button also scroll the user back up to the Route Map near the Weather Situation
Finalise tests
✓    1 waypoint (validate weather information)
✓    5 waypoints (Default) (confirm no errors)
✓    50 waypoints (confirm no missing values)
✓    52 waypoints (Confirm our of range error)
✓    Run forecast, change data (Names, waypoints) re run Forecast

In Progress:
Validate that the flags are displaying the correct speeds

To do:
Improve automated tests
    re-run weather test after user edit values

Stretch goals:
X Allow users to download a PDF report
X In APP Save and load previous routes
X unique id that can be used in app backend to save/load routes

 For testing use this to install frontend dependencies:
  cd frontend
  npm install
  npx playwright install chromium

